// import type { Product } from 'entities/product';
// import data from 'shared/data.json';
//
// export const getProductByProductId = (productId: number): Product | undefined => {
//     return data.find(product => product.id === productId);
// };
